apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: weatherapp-vs
  namespace: default
spec:
  hosts:
  - "weather.165.227.242.247.nip.io"
  gateways:
  - weatherapp-ingress
  http:
  - match:
    - uri:
        exact: /
    route:
    - destination:
        host: weatherappsvc
  - route:
    - destination:
        host: weatherappsvc
